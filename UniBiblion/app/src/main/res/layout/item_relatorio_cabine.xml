<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Ícone da Cabine -->
        <ImageView
            android:id="@+id/img_icone_cabine"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/ic_cabine_fill"
            android:tint="@color/roxo_institucional"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:contentDescription="Ícone de cabine" />

        <!-- Container de Informações -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/img_icone_cabine"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Número da Cabine -->
            <TextView
                android:id="@+id/tv_numero_cabine"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🏢 Cabine: 101"
                android:textColor="@color/black"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Nome do Usuário -->
            <TextView
                android:id="@+id/tv_nome_usuario"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="👤 Reservado por: Nome do Usuário"
                android:textColor="@color/black"
                android:textSize="14sp" />

            <!-- Data da Reserva -->
            <TextView
                android:id="@+id/tv_data_reserva"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="📅 Data: 01/11/2025"
                android:textColor="@color/black"
                android:textSize="13sp" />

            <!-- Período (Hora Início - Hora Fim) -->
            <TextView
                android:id="@+id/tv_periodo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="🕒 Período: 14:00 - 16:00"
                android:textColor="@color/black"
                android:textSize="13sp" />

            <!-- Status da Reserva -->
            <TextView
                android:id="@+id/tv_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="100dp"
                android:minHeight="28dp"
                android:gravity="center"
                android:layout_marginTop="6dp"
                android:text="✅ ATIVA"
                android:textColor="@color/white"
                android:textSize="11sp"
                android:textStyle="bold"
                android:paddingHorizontal="14dp"
                android:paddingVertical="6dp"
                android:background="@drawable/bg_button_purple"
                tools:text="CANCELADA" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
